<Project>
	<PropertyGroup>
		<PackageProjectUrl>https://www.github.com/JJConsulting/JJMasterData</PackageProjectUrl>
		<RepositoryUrl>https://www.github.com/JJConsulting/JJMasterData</RepositoryUrl>
		<PackageIcon>JJMasterData.png</PackageIcon>
		<Version>3.1.20</Version>
		<PackageVersion>$(Version)</PackageVersion>
		<AssemblyVersion>$(Version)</AssemblyVersion>
		<FileVersion>$(Version)</FileVersion>
		<Company>JJ Consulting</Company>
		<Copyright>© 2023 JJConsulting. All rights reserved.</Copyright>
		<PackageReadmeFile>README.md</PackageReadmeFile>
		<PackageReleaseNotes>
                        - bugfix: i18n now works without a database
		        - feat: You can now add a custom bootstrap css
		        - docs: Added docs about authorization
			- breaking_change: You need to call now `MapJJMasterData()` to configure routes
			- bugfix: Fixed number formatting
			- bugfix: DbLogger and FileLogger now uses `IOptionsMonitor` instead of `IOptions` 
		</PackageReleaseNotes>
	</PropertyGroup>
	<ItemGroup>
		<None Include="../../JJMasterData.png">
			<Pack>True</Pack>
			<PackagePath>/</PackagePath>
		</None>
		<None Include="../../README.md">
			<Pack>True</Pack>
			<PackagePath>/</PackagePath>
		</None>
	</ItemGroup>
</Project>

