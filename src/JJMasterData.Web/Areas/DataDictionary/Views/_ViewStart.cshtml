@using JJMasterData.Commons.Options
@using Microsoft.Extensions.Options
@inject IOptions<JJMasterDataCommonsOptions> Settings
@{
    Layout = string.IsNullOrEmpty(Settings.Value.LayoutPath) ? "_MasterDataLayout" : Settings.Value.LayoutPath;
}