@using JJMasterData.Web.Configuration.Options
@using Microsoft.Extensions.Options

@inject IOptionsSnapshot<MasterDataWebOptions> Options
@{
    Layout = string.IsNullOrEmpty(Options.Value.LayoutPath) ? "_MasterDataLayout" : Options.Value.LayoutPath;
}